<!doctype html>
<html lang="id">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Masuk — Sistem HC</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <meta name="theme-color" content="#0d6efd">
    <style>
        body {
            background: #f5f7fb;
        }

        .login-card {
            max-width: 420px;
            margin: 6vh auto;
        }

        .brand {
            font-weight: 700;
            color: #0d6efd;
        }

        .forgot-pass {
            color: var(--bs-secondary);
            cursor: pointer;
        }

        .forgot-pass:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <main class="login-card card shadow-sm p-4">
        <div class="text-center mb-3">
            <div class="mb-2"><img src="assets/logo.svg" width="48" height="48" alt="Sistem HC logo"></div>
            <div class="brand fs-4">Sistem HC</div>
            <div class="small text-secondary">Masuk untuk mengelola data kepegawaian</div>
        </div>

        <div id="loginAlert" class="alert alert-danger d-none" role="alert" aria-live="polite"></div>

        <form action="{{route('/session')}}" method="POST" novalidate>
            {{csrfField()}}
            <div class="mb-3">
                <label class="form-label">Nama Pengguna</label>
                <input type="email" name="email" class="form-control" placeholder="alamat email"
                    value="bimaekaputra@satyaterrabhinneka.ac.id" required />
                <div class="invalid-feedback"><i class="bi bi-exclamation-circle-fill text-danger me-1"></i><span
                        id="loginUserFeedback">Nama pengguna wajib diisi</span></div>
            </div>
            <div class="mb-3">
                <label class="form-label">Kata Sandi</label>
                <input type="password" name="password" class="form-control" placeholder="Kata Sandi" value="bimaeka2000"
                    required />
                <div class="invalid-feedback"><i class="bi bi-exclamation-circle-fill text-danger me-1"></i><span
                        id="loginPassFeedback">Kata sandi wajib diisi</span></div>
            </div>
            <p style="text-align: right;font-size: smaller;" class="forgot-pass">Forgot Password?</p>
            <div class="d-grid mb-2">
                <button class="btn btn-primary" type="submit">Masuk</button>
            </div>

        </form>

        <hr />
        <div class="small text-muted text-center">Hak akses hanya untuk admin yang berwenang.</div>
    </main>

    <!-- Forgot Password Modal -->
    <div class="modal fade" id="forgotModal" tabindex="-1" aria-labelledby="forgotModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="forgotModalLabel">Lupa Kata Sandi</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Tutup"></button>
                </div>
                <form method="POST" class="needs-validation" novalidate>
                    <div class="modal-body">
                        <div id="forgotAlert" class="alert d-none" role="alert" aria-live="polite"></div>
                        <p class="small text-secondary">Masukkan email atau NIP Anda. Kami akan mengirim instruksi
                            pemulihan.</p>
                        <div class="mb-3">
                            <label class="form-label">Email / NIP</label>
                            <input id="forgotInput" class="form-control"
                                placeholder="contoh: user@domain.id atau 1987001" required>
                            <div class="invalid-feedback"><i
                                    class="bi bi-exclamation-circle-fill text-danger me-1"></i>Silakan masukkan email
                                atau NIP yang valid</div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Batal</button>
                        <button id="forgotBtn" type="submit" class="btn btn-primary">Kirim Instruksi</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>


</body>

</html>