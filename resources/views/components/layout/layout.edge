<!DOCTYPE html>
<html lang="id">

<head>
  <meta charset="UTF-8" />

  @if ($slots.meta)
  {{{ await $slots.meta() }}}
  @endif
  <title>
    {{ title || "Your default title" }}
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet" />
  <link rel="icon" type="image/svg+xml"
    href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 16 16'%3E%3Crect x='1' y='1' width='6' height='14' rx='1' fill='%230d6efd'/%3E%3Crect x='9' y='1' width='6' height='6' rx='1' fill='%230d6efd'/%3E%3Crect x='9' y='9' width='6' height='6' rx='1' fill='%230d6efd'/%3E%3C/svg%3E" />
  @stack('style-underconstruction')
  @stack('style-profil')
  {{-- @stack('style-pegawai-detail') --}}

  @vite(['resources/css/styles.css','resources/css/pegawai-detail.css','resources/css/pegawai.css',
  'resources/css/app.css'])

</head>

<body class="bg-body-secondary theme-light-blue" data-bs-theme="light">
  {{-- #NOTE Perubahan untuk develop --}}
  @if(userRole?.role === 'admin' || userRole?.role === 'hrd')
  @include('partials/nav_admin')
  @else
  @include('partials/nav_pegawai')
  @endif
  <div class="main-wrapper d-flex flex-column min-vh-100">
    @include('components/header')
    <main class="flex-grow-1">
      {{{ await $slots.main() }}}
    </main>
    @include('components/footer')
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js">

  </script>
</body>

</html>
